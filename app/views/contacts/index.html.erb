<!DOCTYPE html>
<html lang = "en-US">
<head>
<meta charset = "UTF-8">
</head>
<body>
<h2>Contact Page</h2>
  <div class="container">
    <%= form_for :contact, url: contacts_path do |f| %>
      <p><%= f.label :email %>: <%= f.text_field :email %></p>
      <p><%= f.label :body %>: <%= f.text_field :body %></p>
      <p><%= f.submit %></p>
    <% end %>
</body>
</html>
</html>
<%= link_to 'Back', projects_path %>
</div>
<!-- Contact.column_names for column names. >
